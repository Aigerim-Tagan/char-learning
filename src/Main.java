public class Main {
    public static void main(String[] args) {
        char fstLetter = 'a', scndLetter = 'i', thrdLetter = 'g';
        int fstLetterNum = (int) fstLetter;
        int scndLetterNum = (int) scndLetter;
        int thrdLetterNum = (int) thrdLetter;
        int forthLetterNum = (int) 'e';
        int fifthLetterNum = (int) 'r';
        int sixthLetterNum = (int) 'm';

        System.out.println(fstLetterNum + ", " + scndLetterNum + ", " + thrdLetterNum + ", "
                + forthLetterNum + ", " + fifthLetterNum + ", " + scndLetterNum + ", " + sixthLetterNum);
        char oLetter = 97, twLetter = 105;
        char oLetterNew = (char) oLetter;
        char twLetterNew = twLetter;
        char tLetterNew = (char) thrdLetterNum;
        char fLetterNew = 101;
        char fifLetterNew = 114, sixLetterNew = 109;

        System.out.print(oLetterNew + ", " + twLetterNew + ", " + tLetterNew + ", " + fLetterNew
                + ", " + fifLetterNew + ", " + twLetterNew + ", " + sixLetterNew);
    }
}